VITE_API_BASE_URL=http://localhost:8000
VITE_APP_NAME=YCrispBiscuitTools (dev)
VITE_PUBLIC_FEATURE_FLAG=false

# NOTE: Do not store secrets in frontend environment files. These values are
# exposed to the client and should be safe to commit if they contain no secrets.
